import { WebsocketManager } from './lib/WebsocketManager'

// Start timer to fetch pull requests
// Call add pull request if there is a new pull request
// Update timer every second

class Startup {
  public static main(): void {
    console.log('Hey!')
  }
}

Startup.main()