import { expect } from 'chai'
import { BitbucketController } from '../../controllers/BitbucketController'

// describe('controller: Bitbucket', () => {
//   describe('method: webhook', () => {
//     it('should set ctx status to be 200', async () => {
//       const ctx = await BitbucketController.webhook(ctx)
//     })
//   })
// })
